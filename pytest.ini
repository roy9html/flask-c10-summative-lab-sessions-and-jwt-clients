[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    auth: Tests for authentication
    notes: Tests for notes CRUD
    pagination: Tests for pagination
    access: Tests for access control
